### 📊 Day 5 – Section 9: SELECT Statement, Filtering, and Aggregate Functions

Topics Covered:
- SELECT and FROM basics
- WHERE clause with:
  - Logical operators: AND, OR, NOT
  - Operator precedence
  - IN, NOT IN
  - LIKE, NOT LIKE
  - Wildcard characters (%, _)
  - BETWEEN ... AND ...
  - IS NULL, IS NOT NULL
  - Comparison operators: =, !=, <, >, <=, >=
- SELECT DISTINCT to remove duplicates
- Aggregate Functions:
  - COUNT(), SUM(), AVG(), MIN(), MAX()
- Grouping and Sorting:
  - ORDER BY (ascending/descending)
  - GROUP BY
  - Filtering groups with HAVING
- Aliasing:
  - Column aliases using AS
- Limiting output:
  - LIMIT clause

Key Takeaways:
- Gained strong command over retrieving and filtering data.
- Learned when to use WHERE vs HAVING.
- Practiced writing both row-level and group-level queries.
- Built efficient and readable queries using aliasing and ordering.
- These concepts form the backbone of analytics SQL.

